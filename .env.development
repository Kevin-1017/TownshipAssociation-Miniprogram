# 开发期配置 —— 此文件不含密钥,提交进仓库
# 个人本地覆盖请写 .env.development.local(已被 .gitignore 忽略)

# 走本地 mock,不发真实请求。后端 tsa-api 就绪后改成 false
VITE_USE_MOCK=true

# tsa-api 本地默认端口
VITE_API_BASE_URL=http://localhost:8080/tsa
