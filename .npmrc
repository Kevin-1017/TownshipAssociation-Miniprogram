# ============================================================
#  本项目使用 pnpm(见 package.json 的 packageManager 字段)。
#  误用 npm/yarn 会被 preinstall 的 only-allow 拦下 —— 不是刁难,
#  是为了防止仓库里出现第二份 lockfile 导致两边装出不同依赖树。
# ============================================================

registry=https://registry.npmmirror.com

# ★ 这里刻意**没有** legacy-peer-deps=true。
#   那是 npm 11 的补丁:npm 会尝试满足 pinia 那个 optional 的 Vue-2 peer
#   (@vue/composition-api,要求 vue <2.7),与 uni-app 钉死的 vue 3.4.21 冲突,
#   于是报 ERESOLVE,只能关掉 peer 校验。
#   pnpm 正确识别 optional peer 并跳过未安装的,所以无需关闭任何校验 ——
#   换 pnpm 后 peer 检查是**开着的**,这是实打实的安全收益。
#   推导过程见 docs/TECHNOLOGY.md。
#
# ★ 这里只放 registry 一个通用配置。pnpm 专有的设置项(如 frozen-lockfile)
#   不要写进来 —— preinstall 的 only-allow 会调 npm,而 npm 读到不认识的键
#   会打 "Unknown project config" 警告并声明未来版本将失效。
#   pnpm 自己的配置放 pnpm-workspace.yaml。
