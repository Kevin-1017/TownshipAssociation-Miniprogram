# 为什么需要这一行?(务必读 docs/FAQ.md 的「依赖解析」一节)
#
# @dcloudio/uni-app 硬依赖 @vue/shared@3.4.21,把整棵 Vue 树钉死在 3.4.21。
# pinia 2.2.4 声明的 peer 是 vue "^2.6.14 || ^3.3.0" —— 本身是满足的,
# 但它还带一个 optional 的 peer @vue/composition-api@^1.4.0(Vue 2 专用,
# 要求 vue >=2.5 <2.7)。npm 11 的解析器仍会尝试满足 optional peer,于是报
# ERESOLVE: Conflicting peer dependency。
#
# 这不是真的不兼容:运行时用的是 Vue 3 分支,composition-api 那条路径不会被加载。
# 代价是本项目关闭了 peer 严格校验 —— 引入新依赖前,先确认它的 peer 真的满足,
# 不要依赖 npm 替你报错。
#
# 不要用 pinia 3.x:它要求 vue ^3.5.11,与 uni-app 锁定的 3.4.21 真冲突。
legacy-peer-deps=true

registry=https://registry.npmmirror.com
