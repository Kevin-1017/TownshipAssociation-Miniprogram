# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
*.local

# 个人本地配置,不进仓库(.env.development / .env.production 不含密钥,需提交)
.env.*.local
.claude/settings.local.json

# HBuilderX 构建产物(CLI 模式产物在 dist/,此项为防御性忽略)
unpackage/

# Editor directories and files
.idea
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# husky 生成的运行时装载脚本,不提交
.husky/_

# pnpm
.pnpm-store/
# 注意:pnpm-lock.yaml 必须提交,不要加进这里
